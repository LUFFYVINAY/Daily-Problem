class Solution {

    public int peakElement(int[] arr) {
        // code here
        int   index =0;
        int max=arr[index];
        for(int i=1;i<arr.length;i++){
            if(arr[i]>max){
                index=i;
                max = arr[index];
            }
        }
        return index;
    }
}
