class Solution {
    public int pairWithMaxSum(int[] arr) {
        // code here
        int count=-1;
        for(int i=1;i<arr.length;i++)
        {
            count=Math.max(count,arr[i]+arr[i-1]);
        }
        return count;


    }
}
